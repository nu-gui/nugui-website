# CodeIgniter 4 .gitignore for Production Deployment

# IMPORTANT: vendor and node_modules should be ignored
# Run composer install on the server or include vendor only in deployment branch
/vendor/
/node_modules/

# Writable directories - only ignore contents, not structure
/writable/cache/*
!/writable/cache/index.html
!/writable/cache/.gitkeep
/writable/logs/*
!/writable/logs/index.html
!/writable/logs/.gitkeep
/writable/session/*
!/writable/session/index.html
!/writable/session/.gitkeep
/writable/uploads/*
!/writable/uploads/index.html
!/writable/uploads/.gitkeep
/writable/debugbar/*
!/writable/debugbar/index.html
!/writable/debugbar/.gitkeep

# Temporary files
*.tmp
*.temp
*.bak
*.swp
*.swo
*~
*.log

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# IDE files (optional - can be committed if needed for team)
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Keep test/debug files out
/info.php
/phpinfo.php
/test.php
/debug.php

# Docker files (if not needed for deployment)
docker-compose.override.yml
.dockerignore

# Note: The following are intentionally NOT ignored for cPanel deployment:
# - vendor/ (needed unless Composer is run on server)
# - .env files (will be configured on server)
# - app/ (application source code)
# - public/ (public assets)
# - system/ (CodeIgniter system files)
# - All configuration files
# - .cpanel.yml (deployment configuration)
# - .htaccess (Apache configuration)